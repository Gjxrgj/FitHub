import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    forgotPasswordText: {
        margin: 10,
        textAlign: 'right',
    },
    dontHaveAccountText: {
        margin: 15,
        textAlign: 'center',
    },
});
