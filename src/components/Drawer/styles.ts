import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    drawerContainer: {
        flex: 1,
        padding: 20,
        backgroundColor: '#fff',
        borderLeftWidth: 1,
        borderColor: '#ddd',
    },
    closeText: {
        fontSize: 18,
        fontWeight: 'bold',
        marginBottom: 20,
    },
    drawerItem: {
        fontSize: 16,
        marginVertical: 10,
    },
});
